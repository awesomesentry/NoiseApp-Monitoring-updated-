{
  "name": "noise-monitor",
  "version": "2.0.0",
  "private": true,
  "description": "Smart Classroom Noise Monitor — Node.js backend with static client",
  "scripts": {
    "start": "node server/server.js",
    "dev": "node --watch server/server.js",
    "vercel-build": "echo \"Vercel build complete\""
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0",
    "helmet": "^8.0.0"
  }
}
